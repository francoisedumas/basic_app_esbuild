<% content_for :actions do %>
  <div class="mt-4 flex-shrink-0 flex md:mt-0 md:ml-4">
    <%= link_to t(".new"), new_user_customer_account_path(current_user), class: "btn btn-primary" %>
  </div>
<% end %>

<%= render TableComponent.new(model: CustomerAccount, columns: [:name, :vat_number], collection: @customer_accounts) %>
