<dt class="font-semibold text-gray-800 bg-blue-100 p-2 mb-0 mt-2"><%= term %></dt>
<dd class="flex justify-between bg-blue-100 p-2 mt-0 mb-2">
  <%= description || content %>
  <% if link? %>
    <%= link %>
  <% end %>
</dd>
