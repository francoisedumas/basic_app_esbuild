<dl class="space-y-4 bg-blue-100 p-4 mt-2">
  <dt class="font-semibold text-gray-800"><%= term %></dt>
  <dd class="text-gray-600 flex justify-between">
    <%= description || content %>
    <% if link? %>
      <%= link %>
    <% end %>
  </dd>
</dl>
